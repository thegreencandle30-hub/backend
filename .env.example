# =================================
# The Green Candle Backend Environment Variables
# =================================

# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/green_candle

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# Firebase Configuration (for Authentication)
# Get these from Firebase Console > Project Settings > Service Accounts
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com

# Firebase Cloud Messaging (FCM) for Push Notifications
# Enable/disable FCM functionality
FCM_ENABLED=true
# Legacy Server Key (optional - Admin SDK uses service account)
FCM_SERVER_KEY=your-fcm-server-key

# Dev-only: Allow a fixed test ID token to bypass Firebase in non-production (for local testing)
# Set DEV_AUTH_ENABLED=true and provide a DEV_AUTH_ID_TOKEN to enable
DEV_AUTH_ENABLED=false
DEV_AUTH_ID_TOKEN=dev-test-id-token-123
DEV_AUTH_UID=dev-user-uid
DEV_AUTH_PHONE=+919999999999

# PhonePe Payment Gateway Configuration
# Get these from PhonePe Merchant Dashboard
PHONEPE_MERCHANT_ID=your-merchant-id
PHONEPE_SALT_KEY=your-salt-key
PHONEPE_SALT_INDEX=1
# Set to 'PRODUCTION' for live environment
PHONEPE_ENV=UAT

# Market Data API Configuration
# For live commodity prices (gold, silver, etc.)
MARKET_API_KEY=your-market-api-key
MARKET_API_URL=https://api.example.com/market
# Cache TTL in seconds (default: 60)
MARKET_CACHE_TTL=60

# CORS Configuration
# Frontend admin panel URL
ADMIN_CORS_ORIGIN=http://localhost:3000
